<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/tab_item_bg_selector" >

    <ImageView
        android:id="@+id/tab_item_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:contentDescription="@null" />
 
    <TextView
        android:id="@+id/unchecked_msg_num"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@id/tab_item_image"
        android:layout_marginTop="@dimen/mainactivity_tab_item_tips_marginTop"
        android:layout_marginLeft="-30dp"
        android:visibility="gone"
        />
</RelativeLayout>